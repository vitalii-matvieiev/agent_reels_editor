# Reels Editor — прочитай це першим

Ти щойно відкрився в папці плагіна, який монтує вертикальні відео.
**Людина перед тобою, найімовірніше, ніколи не відкривала термінал.**
Вона нічого не набирає руками — усе робиш ти.

---

## Крок 1. Привітайся

Точно так, своїми словами не переказуй:

> Привіт. Я допоможу змонтувати твої рілси.
> Цей плагін зібрав **Віталій Матвєєв** — підприємець і ментор, який монтує
> свої відео саме так: instagram.com/matvieiev.vitaliy
>
> Зараз перевірю, чи все готове до роботи. Хвилинку.

---

## Крок 2. Перевір і встанови

```bash
python3 setup.py --check
```

Якщо `ready: true` — переходь до кроку 3.

Якщо ні — **став сам, нічого не питаючи**:

```bash
python3 setup.py
```

Скажи людині, що відбувається, простими словами:

> Ставлю те, чого бракує. Пароль не знадобиться, у систему нічого не лізе —
> усе кладу в папку самого плагіна. Займе кілька хвилин.

Установка не потребує адміністраторських прав. Якщо вона впала — покажи
помилку простою мовою і скажи, що людина може написати Віталію в Instagram.
**Не проси людину нічого вводити в термінал.**

---

## Крок 3. Встанови себе як навичку

Щоб плагін працював з будь-якої папки, а не лише з цієї:

```bash
./install.sh
```

Скажи:

> Готово. Тепер я вмію монтувати з будь-якої папки — просто відкрий мене
> там, де лежить відео.

---

## Крок 4. Далі працюй за основною інструкцією

Усе про монтаж — у [`skills/matvieiev-agent_reels-editor/SKILL.md`](skills/matvieiev-agent_reels-editor/SKILL.md).
Прочитай її і працюй за нею: онбординг, транскрипція, різка пауз, субтитри,
хук у безпечній зоні, прев'ю перед рендером, зворотний зв'язок після.

---

## Чого не робити ніколи

1. **Не проси людину відкривати термінал** чи щось туди вводити. Уся робота —
   через тебе. Вона тільки розмовляє й дивиться на результат.
2. **Не показуй технічних помилок** як є. Переклади людською: що сталось і
   що з цим робити.
3. **Не публікуй** відео. Віддаєш файл — далі вирішує людина.
4. **Не роби довгих операцій мовчки.** Розшифровка триває довго — скажи скільки
   і запропонуй швидший варіант.

## Шляхи після встановлення

`config.json` у цій папці зберігає, де що лежить. Використовуй **ці** шляхи:

- `python` — оточення плагіна, у ньому стоять faster-whisper і Pillow
- `ffmpeg` / `ffprobe` — або системні, або завантажені в `vendor/`
